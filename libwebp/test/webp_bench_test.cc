// Copyright 2014 <chaishushan{AT}gmail.com>. All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

#include "test.h"
#include "test_util.h"

#include "jxr.h"

BENCH(jxr, encodeGray8) {
	for(int i = 0; i < BenchN(); ++i) {
		//
	}
}
BENCH(jxr, decodeGray8) {
	for(int i = 0; i < BenchN(); ++i) {
		//
	}
}

BENCH(jxr, encodeGray16) {
	for(int i = 0; i < BenchN(); ++i) {
		//
	}
}
BENCH(jxr, decodeGray16) {
	for(int i = 0; i < BenchN(); ++i) {
		//
	}
}

BENCH(jxr, encodeRGB24) {
	for(int i = 0; i < BenchN(); ++i) {
		//
	}
}
BENCH(jxr, decodeRGB24) {
	for(int i = 0; i < BenchN(); ++i) {
		//
	}
}

BENCH(jxr, encodeRGB48) {
	for(int i = 0; i < BenchN(); ++i) {
		//
	}
}
BENCH(jxr, decodeRGB48) {
	for(int i = 0; i < BenchN(); ++i) {
		//
	}
}

BENCH(jxr, encodeRGBA32) {
	for(int i = 0; i < BenchN(); ++i) {
		//
	}
}
BENCH(jxr, decodeRGBA32) {
	for(int i = 0; i < BenchN(); ++i) {
		//
	}
}

BENCH(jxr, encodeRGBA64) {
	for(int i = 0; i < BenchN(); ++i) {
		//
	}
}
BENCH(jxr, decodeRGBA64) {
	for(int i = 0; i < BenchN(); ++i) {
		//
	}
}
